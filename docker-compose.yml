services:
  frontend:
    image: orderly-dash:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3001}:80"
    networks:
      - orderly-network
    restart: unless-stopped

networks:
  orderly-network:
    name: orderly-network
